server:
  port: 1314

spring:
  application:
    name: modules

######################数据源database######################
  datasource:
    druid:
      stat-view-servlet.enabled: false
      stat-view-servlet.url-pattern: /druid/*
      filter:
        config:
          enabled: false
      web-stat-filter:
        enabled: false
    #   数据源基本配置
    username: root
    password: hellocrw.1103
    driver-class-name: com.mysql.cj.jdbc.Driver
    # &useAffectedRows=true
    url: jdbc:mysql://8.129.110.202:3306/ordersystem?characterEncoding=utf-8&useSSL=true&serverTimezone=Asia/Shanghai
    type: com.alibaba.druid.pool.DruidDataSource
    #   数据源其他配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    #    配置获取连接等待超时的时间
    maxWait: 60000
    #    配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位毫秒
    timeBetweenEvictionRunsMillis: 60000
    #    配置一个连接在池中最小生存的时间，单位毫秒
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    #   打开PSCache，并且指定每个连接上PSCache的大小
    poolPreparedStatements: true
    maxPoolPreparedStatementPerConnectionSize: 20
    #   配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,slf4j
    useGlobalDataSourceStat: true
    #   通过connectProperties属性来打开mergeSql功能；慢SQL记录
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

######################redis######################
  redis:
    port: 6379
    host: 8.129.110.202
    password:
    jedis:
      pool:
        max-active: 8
        min-idle: 0
        max-wait: -1
        max-idle: 8
    timeout: 30000
    database: 3

mybatis:
  mapper-locations: classpath*:xml/*.xml
  type-aliases-package: crw.demo.model.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl